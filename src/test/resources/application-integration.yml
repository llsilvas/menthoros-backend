spring:
  datasource:
    url: jdbc:postgresql://192.168.15.24:5432/menthoros_test
    username: postgres
    password: postgres
  jpa:
    hibernate:
      ddl-auto: validate
  flyway:
    enabled: true

logging:
  level:
    org.flywaydb: WARN
    org.hibernate.SQL: WARN
