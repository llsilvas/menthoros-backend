# ========================================
# PERFIL: local  (Docker Compose local)
# ========================================
spring:
  config:
    activate:
      on-profile: local
  jpa:
    hibernate:
      ddl-auto: validate

# ========================================
# CORS CONFIGURATION (local)
# ========================================
app:
  cors:
    allowed-origins: ${CORS_ALLOWED_ORIGINS:http://localhost:5173,http://localhost:5174,http://localhost:3000}
