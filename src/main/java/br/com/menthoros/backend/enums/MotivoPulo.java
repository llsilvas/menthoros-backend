package br.com.menthoros.backend.enums;

/**
 * Por que o atleta pulou o treino de hoje ("Não vou conseguir hoje"). Lista curta e opcional de
 * propósito: é o dado que o coach usa para ajustar a semana, e não pode virar fricção.
 */
public enum MotivoPulo {
    SEM_TEMPO,
    CANSADO,
    DOR,
    OUTRO
}
